# Security

Please do not report security vulnerabilities in public issues.

Email `info@cuebook.app` with a description, reproduction steps, and the
affected version. We will acknowledge the report and coordinate disclosure.

The CLI stores OAuth credentials locally with owner-only file permissions. It
never asks for exchange API keys, wallet keys, or real-money trading
credentials.
